# Generated by setup.sh — never commit real secrets
.env
config/config.json
nginx/ssl/
livekit/livekit.yaml
configs.txt
docker-compose.override.yml

# Cloned source tree (built during setup)
fluxer-src/

# Let's Encrypt certificates (stored in Docker volumes, not needed here)
certbot/

# Editor & OS
.DS_Store
*.swp
*.swo
Thumbs.db
